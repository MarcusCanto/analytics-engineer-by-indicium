version: 2

models:
  - name: dim_products
    description: table with products information
    columns:
      - name: PK_PRODUCT
        description: dim product primary key
        tests:
          - unique
      - name: NM_PRODUCT
        description: product name