version: 2

models:
  - name: dim_products
    description: table with products information
    columns:
      - name: PK_PRODUCT
        description: dim produc primary key
      - name: NM_PRODUCT
        description: product name