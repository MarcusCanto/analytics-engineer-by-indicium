version: 2

models:
  - name: dim_sales_reason
    description: table with information about the reason of sales
    columns:
        - name: FK_SALES_ORDER_HEADER
          description: table sales order header primary key 
        - name: PK_SALES_REASON
          description: table sales reason header primary key
        - name: NM_SALES_REASON
          description: sales reason name
